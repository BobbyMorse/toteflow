# The Racing API (UK/IRE feeds) — optional
RACING_API_USER=
RACING_API_PASS=

# Betfair Exchange — optional
BETFAIR_APP_KEY=
BETFAIR_SESSION_TOKEN=

# Only set at BUILD time if serving behind a reverse-proxy path prefix
# (e.g. "/toteflow"). Leave blank for root-path deploys like Fly.
NEXT_BASE_PATH=
